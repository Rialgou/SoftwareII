{/*

import React from 'react';
import { Scrollbars } from 'react-custom-scrollbars';

const ScrollBar = (props) => {
  return (
    <Scrollbars
      style={{ height: '100vh' }} // Altura de la barra de desplazamiento personalizada
      autoHide // Ocultar automáticamente la barra de desplazamiento cuando no se necesita
      renderThumbVertical={({ style, ...props }) => (
        <div
          {...props}
          style={{
            ...style,
            backgroundColor: '#333', // Color del pulgar de la barra de desplazamiento
            borderRadius: '5px', // Radio de borde del pulgar de la barra de desplazamiento
          }}
        />
      )}
    >
      {props.children}
    </Scrollbars>
  );
};

export default ScrollBar;


*/}
